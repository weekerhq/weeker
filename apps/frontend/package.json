{
  "name": "@weekerhq/frontend",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "ci": "echo 'done'",
    "init": "pnpm --filter . --frozen-lockfile install",
    "dev": "astro dev --host",
    "build": "astro build"
  },
  "dependencies": {
    "@weekerhq/asset-logo": "workspace:*",
    "astro": "^5.3.0"
  }
}
